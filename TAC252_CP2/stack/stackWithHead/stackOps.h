
#include "stack.h"

extern StackTop createStack();

extern StackTop push(Element e, StackTop st);

extern StackTop pop(StackTop st);

extern Element top(StackTop st);

extern void printStack(StackTop st);

