#include<stdio.h>


int main()

	{


	
	 int i=10;

	
	 printf("the value of i = %d and the address of MEM where i is stored is %u\n",i, &i);
	
	 printf("The size of memory allocated for i is %d\n",sizeof(i));

	return 0;

	}
